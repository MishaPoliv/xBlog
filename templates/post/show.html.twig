{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ post.Caption }}</h1>
        <hr>
    <p> {{ post.DatePublic|date("m/d/Y") }}</p>
        <hr>
    <p>{{ post.xText }}</p>
        <hr>

    <a href="{{ path('post_edit', {id: post.id}) }}">
        Edit post
    </a>
    <br>
    <a href="{{ path("homepage") }}">
         Back to Homepage
    </a>

{% endblock %}

{% block title %}

{% endblock %}